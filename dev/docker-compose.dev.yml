version: "3"
services:
  curator:
    command: "npm run-script dev"
    volumes:
      - ../verification/curator-service/api/src:/usr/src/app/src
  data:
    command: "npm run-script dev"
    volumes:
      - ../data-serving/data-service/src:/usr/src/app/src
