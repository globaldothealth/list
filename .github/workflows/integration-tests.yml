name: Integration Tests CI

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/integration-tests.yml"
      - "dev/docker-compose.yml"
      - "verification/curator-service/**"
      - "data-serving/data-service/**"
      - "data-serving/samples/**"
  pull_request:
    paths:
      - ".github/workflows/integration-tests.yml"
      - "dev/docker-compose.yml"
      - "verification/curator-service/**"
      - "data-serving/data-service/**"
      - "data-serving/samples/**"

jobs:
  integration-tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        browser: ["chrome", "firefox"]
    name: Cypress tests on ${{ matrix.browser }}

    steps:
      - uses: actions/checkout@v2
      - name: Configure AWS profile
        run: |
          docker run -rm -it amazon/aws-cli configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }} --profile default
          docker run -rm -it amazon/aws-cli configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }} --profile default
          docker run -rm -it amazon/aws-cli configure set default.region us-east-2
      - name: Build stack
        run: docker-compose -f dev/docker-compose.yml -f dev/docker-compose.ci.yml up --build -d -V
      - uses: cypress-io/github-action@v2
        with:
          working-directory: verification/curator-service/ui
          browser: ${{ matrix.browser }}
          wait-on: "http://localhost:3002"
          wait-on-timeout: 300
        env:
          CI: true
      # Screenshots are only available on failures.
      - uses: actions/upload-artifact@v1
        if: failure()
        with:
          name: cypress-screenshots
          path: verification/curator-service/ui/cypress/screenshots
      # Videos are always available.
      - uses: actions/upload-artifact@v1
        if: always()
        with:
          name: cypress-videos
          path: verification/curator-service/ui/cypress/videos/components
