name: Docker Compose Actions Workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker-compose -f dev/docker-compose.yml -d up
      - name: Test
        run: docker run --network container:curator appropriate/curl -s --retry 10 --retry-connrefused http://localhost:3001/
