{
  "extends": "../../common/tsconfig.json",
  "compilerOptions": {
    "outDir": "dist", /* Redirect output structure to the directory. */
  },
  "include": [
    "src/**/*.ts",
    "**/*.test.ts"
  ]
}